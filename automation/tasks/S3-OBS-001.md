# S3-OBS-001

## Goal
Implement event replay JSONL parsing and stats computation.

## Scope
- In `src/replay.zig`:
  - Implement `loadEvents()`: parse JSONL file line by line, deserialize each `EventRecord`, return a list.
  - Implement `processEvent()`: update `ReplaySession` state from an event (increment counters, track timing).
  - Implement `computeStats()`: compute `SessionSummary` from actual event data (total events, duration, event kind breakdown).
- Remove the 4 TODO comments (lines 61, 192, 196, 234).
- Add tests with sample JSONL input verifying correct parsing and stats.

## Acceptance
- `loadEvents()` parses JSONL and returns event list
- `processEvent()` updates session state
- `computeStats()` returns real summary from events
- All TODOs removed from replay.zig
- Tests verify round-trip: serialize events → write JSONL → load → compute stats
- All existing tests still pass
