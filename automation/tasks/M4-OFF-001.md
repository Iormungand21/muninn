# M4-OFF-001

## Goal
Complete the offline queue with JSONL persistence, drain loop, and bounds checking.

## Scope
- In `src/offline.zig`:
  - Implement `enqueue()` fully: check `max_items` capacity, deduplicate by item ID, serialize to JSONL.
  - Implement `drain()` fully: read JSONL file, parse each line into `QueueItem`, return items sorted by priority then timestamp.
  - Add `drainBatch()` for batch processing with configurable batch size.
  - Remove the 3 TODO comments (lines 214, 229, 255).
- In `src/config_types.zig`: verify `OfflineQueueConfig.max_items` has a sensible default (e.g., 1000).
- Add tests: enqueue at capacity returns error, dedup by ID, drain returns correct order, empty drain returns empty slice.

## Acceptance
- `enqueue()` enforces max_items and deduplicates
- `drain()` parses JSONL and returns sorted items
- All 3 TODOs removed
- Tests cover capacity, dedup, ordering, empty state
- All existing offline tests still pass
