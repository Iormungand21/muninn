# M3-DEL-001

## Goal
Add a remote planning delegation client stub so `muninn` can ask `huginn` for plans.

## Scope
- Define request/response types for remote planning.
- Add a client interface/stub (HTTP transport optional placeholder).
- No production auth or retry logic required yet.

## Acceptance
- Delegation API shape is defined and documented
- Stub client compiles/syntax-valid
