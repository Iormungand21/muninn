# H2-MEM-001

## Goal
Create `huginn`-side snapshot import/export pipeline skeleton for memory hub duties.

## Scope
- Add snapshot API/types for import/export versioned payloads (skeleton acceptable).
- Wire minimal CLI or internal entrypoint for future sync tasks.
- No remote transport required yet.

## Acceptance
- Versioned snapshot payload type exists
- Import/export function stubs or minimal working path implemented and documented
