# M4-DEL-001

## Goal
Wire delegation client HTTP transport for plan requests and polling.

## Scope
- In `src/delegation.zig`:
  - Implement `requestPlan()`: HTTP POST to huginn's plan endpoint using `http_util.curlPost` (or equivalent). Serialize `PlanRequest` as JSON body, parse `PlanResponse` from response.
  - Implement `pollResult()`: HTTP GET to huginn's status endpoint, parse `PlanResponse`.
  - Implement `healthCheck()`: HTTP GET to huginn's health endpoint, return bool.
  - Remove the 3 TODO comments (lines 224, 237, 250).
- Add a timeout configuration (default 30s for requests, 5s for health checks).
- Add tests with mock/expected JSON payloads verifying serialization and deserialization.

## Acceptance
- `requestPlan()` makes real HTTP POST with correct JSON body
- `pollResult()` makes HTTP GET and parses response
- `healthCheck()` returns true/false based on HTTP status
- All 3 TODOs removed
- Tests cover JSON round-trip and error paths
- All existing delegation tests still pass
